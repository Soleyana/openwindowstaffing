# StaffingRequest – Next Milestone

Current: minimal model + `POST /api/staffing-requests`, `GET /api/staffing-requests`.

## Planned additions
- Line items (role, quantity, rate, dates)
- PO number, billing contact
- Status workflow: draft → submitted → approved → in_progress → filled
- Link to Jobs / Applications
- Invoice generation
- Frontend: create/list staffing requests in Orders or dedicated page
